{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + native folder picker for Palma's main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
